# Python MySQL Generator Project

This project demonstrates how to create a Python generator that streams rows from a MySQL database table (`user_data`) one by one.  

It includes scripts to create a database, populate it with sample CSV data, and retrieve data efficiently using a generator.
